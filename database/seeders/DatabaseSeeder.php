<?php

namespace Database\Seeders;

// use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

use App\Models\User;
use App\Models\Club;
use App\Models\Activity;
use App\Models\ClubGallery;
use App\Models\Member;
use App\Models\Budget;
use App\Models\Document;
use App\Models\Post;
use App\Models\ClubRequest;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     */
    public function run(): void
    {
        // Seeder cho bảng users
        // User::factory(10)->create();

        // Seeder cho bảng clubs
        // Club::factory(5)->create();

        // Seeder cho bảng activities
        // Activity::factory(10)->create();

        // Seeder cho bảng club_galleries
        // ClubGallery::factory(20)->create();

        // Seeder cho bảng members
        // Member::factory(20)->create();

        // Seeder cho bảng budgets
        // Budget::factory(15)->create();

        // Seeder cho bảng budget_documents
        // Document::factory(10)->create();

        // Seeder cho bảng posts
        // Post::factory(10)->create();

        // Seeder cho bảng club_requests
        // ClubRequest::factory(5)->create();
    }
}
